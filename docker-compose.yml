services:
  mongo:
    image: mongo:7
    container_name: moneyline-mongo
    ports:
      - "27017:27017"
    volumes:
      - moneyline_mongo_data:/data/db
    restart: unless-stopped

volumes:
  moneyline_mongo_data:
